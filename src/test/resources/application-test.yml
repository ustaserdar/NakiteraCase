spring:
  datasource:
    url: jdbc:h2:mem:nakitera-test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
  jpa:
    hibernate:
      ddl-auto: create-drop
  h2:
    console:
      enabled: false
